apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: tunestack-api
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: tunestack-api
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
